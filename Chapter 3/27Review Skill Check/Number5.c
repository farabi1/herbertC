#include<stdio.h>
int main(){
    
    int num;
    printf("Enter the number of proverbs :");
    scanf("%d",&num);  
 
        if(num>=1)printf("1. A stitch in time saves nine.\n");
        if(num>=2)printf("2. Actions speak louder than words.\n\n");
        if(num>=3)printf("3. Don't count your chickens before they hatch.\n");
        if(num>=4)printf("4. All good things come to those who wait.\n");
        if(num>=5)printf("5. The early bird catches the worm.\n");
    
    
    return 0;

}

//clear
