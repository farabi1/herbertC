// if(count != 0) ..not written in this way 
// but in that way 
// if(count)

// because true is any nonzero value and false is zero.

// and statement like these

// if(count==0)... are written in these
// if(!count)....

// the expression !count is true only if count is zero.

//Clear